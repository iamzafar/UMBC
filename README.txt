we can upload any project related document here.

Some tips about github:
- to check the local repository status type: "git status"
- to track and add all files into local repository: "git add ." or "git add -A"
- to commit all changes: "git commit -m "type some message" "
- to push your code: "git push origin master" or "git push"